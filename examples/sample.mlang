# Sample MathLang script

# Variables
x = 10
y = 20

# Basic arithmetic
x + y
x * y

# Using functions
Sqrt(x^2 + y^2)
Sin([[PI]]/4)

# Lists and higher-order functions
numbers = List(1, 2, 3, 4, 5)
doubled = Map(numbers, n -> n * 2)
doubled

# Sum of squares
Sum(Map(numbers, n -> n^2))
